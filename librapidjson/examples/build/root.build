cxx.std = latest

using cxx

hxx{*}: extension = h
cxx{*}: extension = cpp

# Examples are built with the package but are not tests.
#
exe{*}: test = false

# The test target for cross-testing (running tests under Wine, etc).
#
test.target = $cxx.target
